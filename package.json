{
  "scripts": {
    "start:back-end": "cd bigbyte && cd Backend && npm install && npm start",
    "start:front-end": "cd bigbyte && npm install && npm start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "firebase": "^10.12.3",
    "firebase-admin": "^12.2.0"
  }
}
